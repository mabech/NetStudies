@model IEnumerable<NewLaunch.Models.Voted>

@{
    ViewBag.Title = "Vote List";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Employee.Name)*@ Employee Name
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Restaurant.Name)*@ Restaurante Name
        </th>
        <th>
        @Html.DisplayNameFor(model => model.DateVoted)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Restaurant.Name)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.DateVoted)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
